# API artifacts

The API generates its v1 OpenAPI document at `/openapi/v1.json`. A checked-in generated artifact and client generation policy will be added once the WP-02 application surface is established.
