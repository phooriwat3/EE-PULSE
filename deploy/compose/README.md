# Compose deployment assets

The WP-01 development skeleton is at the repository root in `docker-compose.yml`. Production hardening, reverse proxy integration, resource limits, backup, and upgrade procedures are owned by WP-10.
